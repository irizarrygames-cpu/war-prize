{
  "name": "war-prize",
  "version": "1.0.0",
  "private": true,
  "description": "Choose one of two cards, matching numbers cancel, highest unique wins the Prize Card.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0"
  }
}
